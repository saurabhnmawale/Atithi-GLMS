name: atithi_glms
description: Atithi — Guest Lifecycle Management System for Event Hospitality.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Database
  drift: ^2.19.1
  drift_flutter: ^0.2.2
  sqlite3_flutter_libs: ^0.5.24

  # State management
  flutter_riverpod: ^2.5.1

  # Excel export
  syncfusion_flutter_xlsio: ^25.1.35

  # File operations
  file_picker: ^8.1.2
  share_plus: ^12.0.1
  path_provider: ^2.1.3

  # CSV parsing
  csv: ^6.0.0

  # Date/time formatting
  intl: ^0.19.0

  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.19.1
  build_runner: ^2.4.11
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
